# ============================================================
# APP PLATFORM REFERENCE (.env.remote) — NOT used by any automation
# This documents the env vars configured on DigitalOcean App Platform.
# Secrets are managed in the App Platform UI; this file is for reference only.
# Copy to .env.remote and fill in for your own deployment reference.
# ============================================================

# Agent identity
AGENT_ID=agent-cattle-mode
AGENT_NAME=OpenClaw Cattle Mode

# Telegram
TELEGRAM_BOT_TOKEN=<secret — set in App Platform UI>
TELEGRAM_ALLOWFROM=6944791786

# Providers
GRADIENT_API_KEY=<secret — set in App Platform UI>
AGENT_DEFAULT_MODEL=

# Node.js heap (prevents OOM on 2GB instances)
NODE_OPTIONS=--max-old-space-size=1536

# Gateway auth
OPENCLAW_GATEWAY_TOKEN=<secret — set in App Platform UI>

# DigitalOcean Spaces
SPACES_BUCKET=openclaw-cattle-mode-syd1
SPACES_REGION=syd1
SPACES_ACCESS_KEY_ID=<secret — set in App Platform UI>
SPACES_SECRET_ACCESS_KEY=<secret — set in App Platform UI>
# Note: S3_ENDPOINT_URL is NOT set for App Platform (uses default DO Spaces endpoint)
