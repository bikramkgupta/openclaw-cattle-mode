# ============================================================
# APP PLATFORM DEPLOY (.env.remote) — used by scripts/deploy.sh
# Copy to .env.remote and fill in your values.
# Values are substituted into app.yaml at deploy time.
# This file is gitignored — never commit it.
# ============================================================

# GHCR image tag for App Platform (pinned tag; use 2026.2.9 to match OpenClaw version)
IMAGE_TAG=2026.2.9

# Agent identity
AGENT_ID=agent-cattle-mode
AGENT_NAME=OpenClaw Cattle Mode

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWFROM=6944791786

# Providers
GRADIENT_API_KEY=
AGENT_DEFAULT_MODEL=

# Node.js heap (prevents OOM on 2GB instances)
NODE_OPTIONS=--max-old-space-size=1536

# Gateway auth — generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# DigitalOcean Spaces — create key with:
#   doctl spaces keys create "my-key" --grants "bucket=<bucket>;permission=readwrite" --output json
SPACES_BUCKET=openclaw-cattle-mode-syd1
SPACES_REGION=syd1
SPACES_ACCESS_KEY_ID=
SPACES_SECRET_ACCESS_KEY=
