# ============================================================
# LOCAL DEVELOPMENT (.env.docker) — used by docker-compose.yml
# Copy this file to .env.docker and fill in required values.
# Usage: docker compose --env-file .env.docker up -d
# ============================================================

# OpenClaw / image version (aligns with .env.remote IMAGE_TAG; local builds from Dockerfile)
IMAGE_TAG=2026.2.9

# Telegram (required)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWFROM=6944791786

# WhatsApp (personal number — E.164 format, e.g. +14085551234)
WHATSAPP_ALLOWFROM=

# Skills — ClawHub slugs, comma-separated (e.g. weather,file-search)
# Installed on every boot via: npx clawhub@latest install <slug>
# Browse available skills at https://clawhub.ai
OPENCLAW_SKILLS=weather

# Gradient (required)
GRADIENT_API_KEY=

# Optional model providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional override (otherwise: gradient/anthropic-claude-opus-4.6)
AGENT_DEFAULT_MODEL=

# Node.js heap — 768 for 1gb, 1536 for 2gb (512 OOMs on doctor)
NODE_OPTIONS=--max-old-space-size=1536

# Agent identity
AGENT_ID=agent-dev
AGENT_NAME=OpenClaw Dev Agent

# Gateway auth (required) — generate: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Object store — RustFS local defaults (do not change for local dev)
SPACES_BUCKET=openclaw-dev
SPACES_REGION=us-east-1
SPACES_ACCESS_KEY_ID=rustfsadmin
SPACES_SECRET_ACCESS_KEY=rustfsadmin
S3_ENDPOINT_URL=http://rustfs:9000

# RustFS image (optional override)
RUSTFS_IMAGE=rustfs/rustfs:1.0.0-alpha.82
